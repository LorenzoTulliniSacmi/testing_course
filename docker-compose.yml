services:
  mongodb:
    image: mongo:7
    container_name: kanban-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=kanban

volumes:
  mongodb_data:
